{
  "name": "@app/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/server.ts",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "dev": "tsx watch src/server.ts",
    "lint": "eslint .",
    "test": "echo 'no tests yet'",
    "clean": "rm -rf dist .turbo",
    "db:generate": "prisma generate --schema=../../prisma/schema.prisma --env-file=../../.env",
    "db:validate": "prisma validate --schema=../../prisma/schema.prisma --env-file=../../.env",
    "db:format": "prisma format --schema=../../prisma/schema.prisma --env-file=../../.env"
  },
  "prisma": {
    "schema": "../../prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "fastify": "^5.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "prisma": "^6.19.3",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
